# CLI Index

Auto-generated by `python -m tools.cli_index`. Do not edit by hand — regenerate after adding a CLI.

**Total CLIs:** 20

## Contents

| Module | Directory | Description |
| --- | --- | --- |
| `tools.anonymize_cli` | `tools/` | Anonymize a copy of the SQLite state DB so it's safe to share with a teammate, populate staging, or hand to QA. |
| `tools.backtests` | `tools/` | tools/backtests.py — run every platform backtest in one command. |
| `tools.backup_cli` | `tools/` | Backup + restore CLI for the Ship Tracker SQLite state DB and the generated-reports tree. |
| `tools.changelog_cli` | `tools/` | Regenerate CHANGELOG.md from the git log. Default window: the last 90 days, grouped by date. |
| `tools.cli_index` | `tools/` | Walk tools/ + cli/ and emit a CLI index markdown + JSON registry. Use --verify in CI to gate doc drift. |
| `tools.completion_cli` | `tools/` | Generate bash / zsh tab-completion scripts for the project's argparse-based CLIs. |
| `tools.db_check_cli` | `tools/` | DB integrity check + auto-fix CLI for the Ship Tracker SQLite state database. |
| `tools.multi_container_snapshot_cli` | `tools/` | Run the daily port-supply snapshot job for every container type in one pass. Per-container failures don't poison the rest of the fan-out. |
| `tools.openapi_cli` | `tools/` | Generate / validate the Ship Tracker OpenAPI 3.0 spec. |
| `tools.ops` | `tools/` | Entry point so ``python -m tools.ops`` works the same as |
| `tools.ops_cli` | `tools/` | Operator CLI for Ship Tracker admin tasks. |
| `tools.port_supply_bulk_diff` | `tools/` | Aggregate Port Supply Lines snapshot diffs over a window (e.g. last week) and rank ports by volatility. |
| `tools.port_supply_diff` | `tools/` | Diff two Port Supply Lines summary snapshots. |
| `tools.port_supply_export` | `tools/` | tools/port_supply_export.py — CLI bulk-export for Port Supply Lines CSVs. |
| `tools.replay_cli` | `tools/` | Replay historical alerts to a delivery channel without consuming budget. |
| `tools.schema_docs_cli` | `tools/` | Generate Markdown / JSON documentation of the SQLite schema from a live DB + migration history. |
| `tools.snapshot_integrity_cli` | `tools/` | Check port-supply snapshot files for completeness + parse warnings. |
| `tools.styles_audit` | `tools/` | Design-system audit tool. |
| `tools.supply_shock_cli` | `tools/` | Quantify the company-level impact of pre-canned supply-line shocks (Suez closure, Shanghai 50%, Panama drought, etc.). |
| `tools.test_flakiness_cli` | `tools/` | Track + surface flaky tests from pytest JUnit XML. |

## `tools/`

### `tools.anonymize_cli`

Anonymize a copy of the SQLite state DB so it's safe to share with a teammate, populate staging, or hand to QA.

```
python -m tools.anonymize_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--dry-run` |  | false |  | Show what would change without writing the output file. Source DB is opened READ-ONLY; no mutation happens. |
| `--output` |  | — |  | Path where the anonymized copy is written. Required unless --dry-run is set. |
| `--profile` |  | `standard` | `standard`, `aggressive`, `redact-only` | Anonymization profile. 'standard' scrubs PII + secrets but keeps alert/report shape. 'aggressive' also empties annotation bodies + audit detail_json. 'redact-only' replaces strings but preserves row counts. Default: standard. |
| `--source` | yes | _required_ |  | Path to the source SQLite DB (opened READ-ONLY). |
| `--verbose` |  | false |  | Print per-table scrubbed/dropped counts to stderr. |

### `tools.backtests`

tools/backtests.py — run every platform backtest in one command.

```
python -m tools.backtests
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--compare-baseline` |  | — |  | Run the validators and report drift vs the JSON snapshot at PATH (previously written by --save-baseline). Exits 1 on any healthy-flag flip or raw-field drift beyond the per-metric tolerance. |
| `--format` |  | `text` | `text`, `json`, `markdown` | Output format (default: text). |
| `--save-baseline` |  | — |  | Run the validators and write a JSON snapshot to PATH. The snapshot captures every validator's healthy flag + headline + raw_fields and is the reference for --compare-baseline. Does not print the normal report. |
| `--strict` |  | false |  | Exit 1 if any validator reports healthy=False. Useful as a CI gate. |
| `--verbose` |  | false |  | Include per-class scorecard rows under each validator in the text output (no effect on json/markdown formats). Useful for triage when a validator goes red. |

### `tools.backup_cli`

Backup + restore CLI for the Ship Tracker SQLite state DB and the generated-reports tree.

```
python -m tools.backup_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `<create>` |  | — |  | subcommand: create |
| `<list>` |  | — |  | subcommand: list |
| `<restore>` |  | — |  | subcommand: restore |
| `<verify>` |  | — |  | subcommand: verify |

### `tools.changelog_cli`

Regenerate CHANGELOG.md from the git log. Default window: the last 90 days, grouped by date.

```
python -m tools.changelog_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--group-by` |  | `date` | `date`, `category`, `flat` | How to group commits in the rendered Markdown (default: date). |
| `--limit` |  | — |  | Max number of commits to include (default: no cap). |
| `--out` |  | — |  | Output file path (default: /Users/aaronmortara/MC/Models/Ship/CHANGELOG.md). |
| `--print` |  | false |  | Print to stdout instead of writing a file. |
| `--since` |  | `90d` |  | Lower-bound cutoff. Accepts an absolute date (`2026-01-01`), a git relative string (`2 weeks ago`), or the shorthand `30d` / `12w` / `6m` / `1y` (default: 90d). |
| `--title` |  | `Changelog` |  | Top-level title (default: Changelog). |
| `--until` |  | — |  | Upper-bound cutoff. Same shape as --since (default: none). |

### `tools.cli_index`

Walk tools/ + cli/ and emit a CLI index markdown + JSON registry. Use --verify in CI to gate doc drift.

```
python -m tools.cli_index
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--out-json` |  | `docs/cli_registry.json` |  | JSON registry output path (default: docs/cli_registry.json). |
| `--out-md` |  | `docs/CLI_INDEX.md` |  | Markdown output path (default: docs/CLI_INDEX.md). |
| `--quiet` |  | false |  | Suppress stdout summary (useful for cron / CI). |
| `--verbose` |  | false |  | Log discovery failures to stderr (each CLI that falls through to a placeholder entry). |
| `--verify` |  | false |  | Verify-only mode — no writes. Exits 1 if the on-disk files differ from what the discoverer would produce. The CI gate. |

### `tools.completion_cli`

Generate bash / zsh tab-completion scripts for the project's argparse-based CLIs.

```
python -m tools.completion_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `<all>` |  | — |  | subcommand: all |
| `<bash>` |  | — |  | subcommand: bash |
| `<zsh>` |  | — |  | subcommand: zsh |

### `tools.db_check_cli`

DB integrity check + auto-fix CLI for the Ship Tracker SQLite state database.

```
python -m tools.db_check_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--db` |  | — |  | Path to a specific .db file. Default: live cache/ship_tracker.db as resolved through state.db.DB_PATH. |
| `--fix` |  | false |  | Auto-fix safe issues: mark expired api_tokens inactive, mark expired invitations consumed, delete alert_silences expired >30 days. Does NOT touch integrity_check or foreign_key_check failures. |
| `--full` |  | false |  | Run the deeper PRAGMA integrity_check (page-by-page scan) instead of the cheaper quick_check. Slow on multi-GB DBs. |
| `--json` |  | false |  | Emit machine-readable JSON instead of the text report. |

### `tools.multi_container_snapshot_cli`

Run the daily port-supply snapshot job for every container type in one pass. Per-container failures don't poison the rest of the fan-out.

```
python -m tools.multi_container_snapshot_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--container-types` |  | — |  | Comma-separated list of container types to snapshot (e.g. '40FT_DRY,40FT_REEFER'). Defaults to {','.join(DEFAULT_CONTAINER_TYPES)}. |
| `--format` |  | `text` |  | Output format (default: text). |
| `--min-diff-delta-days` |  | `1.0` |  | Per-port deficit delta threshold for the diff comparator (default: 1.0). |
| `--root` |  | — |  | Override the snapshot tree root. Defaults to processing.port_supply_history.SNAPSHOT_ROOT. |
| `--today` |  | — |  | Snapshot date in YYYY-MM-DD format. Defaults to UTC today. |

### `tools.openapi_cli`

Generate / validate the Ship Tracker OpenAPI 3.0 spec.

```
python -m tools.openapi_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `<json>` |  | — |  | Write the spec as JSON (default: docs/openapi.json). |
| `<validate>` |  | — |  | Smoke-validate the spec (exits non-zero on error). |
| `<yaml>` |  | — |  | Write the spec as YAML (default: docs/openapi.yaml). |
| `--out` |  | — |  | Output path (default: docs/openapi.json). |
| `--out` |  | — |  | Output path (default: docs/openapi.yaml). |

### `tools.ops`

Entry point so ``python -m tools.ops`` works the same as
``python -m tools.ops_cli``.

```
python -m tools.ops
```

_NOTE: introspection failed — see the module's docstring for usage. Discovery error logged in the JSON registry._

### `tools.ops_cli`

Operator CLI for Ship Tracker admin tasks.

```
python -m tools.ops
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `<alerts>` |  | — |  | subcommand: alerts |
| `<annotations>` |  | — |  | subcommand: annotations |
| `<anomalies>` |  | — |  | subcommand: anomalies |
| `<audit>` |  | — |  | subcommand: audit |
| `<calendar>` |  | — |  | subcommand: calendar |
| `<channels>` |  | — |  | subcommand: channels |
| `<digest>` |  | — |  | subcommand: digest |
| `<disruption>` |  | — |  | subcommand: disruption |
| `<escalations>` |  | — |  | subcommand: escalations |
| `<export>` |  | — |  | subcommand: export |
| `<filters>` |  | — |  | subcommand: filters |
| `<health-alerts>` |  | — |  | subcommand: health-alerts |
| `<health>` |  | — |  | subcommand: health |
| `<incidents>` |  | — |  | subcommand: incidents |
| `<invite>` |  | — |  | subcommand: invite |
| `<mfa>` |  | — |  | subcommand: mfa |
| `<perf-budgets>` |  | — |  | subcommand: perf-budgets |
| `<perf>` |  | — |  | subcommand: perf |
| `<prefs>` |  | — |  | subcommand: prefs |
| `<reports>` |  | — |  | subcommand: reports |
| `<retries>` |  | — |  | subcommand: retries |
| `<rules>` |  | — |  | subcommand: rules |
| `<schedules>` |  | — |  | subcommand: schedules |
| `<settings>` |  | — |  | subcommand: settings |
| `<silences>` |  | — |  | subcommand: silences |
| `<status>` |  | — |  | subcommand: status |
| `<telemetry>` |  | — |  | subcommand: telemetry |
| `<tokens>` |  | — |  | subcommand: tokens |
| `<users>` |  | — |  | subcommand: users |

### `tools.port_supply_bulk_diff`

Aggregate Port Supply Lines snapshot diffs over a window (e.g. last week) and rank ports by volatility.

```
python -m tools.port_supply_bulk_diff
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--container-type` |  | `40FT_DRY` |  | Container type to walk (default 40FT_DRY). |
| `--format` |  | `text` | `text`, `json`, `markdown` | Output format (default: text). |
| `--min-delta` |  | `1.0` |  | Per-pair minimum \|deficit_days\| to count as a material move (default 1.0). Mirrors the --min-delta knob on the per-day diff CLI. |
| `--out` |  | — |  | Write the report to this path instead of stdout. |
| `--root` |  | — |  | Snapshot root override (defaults to the SNAPSHOT_ROOT in processing.port_supply_history). Useful for tests + ad-hoc replay of an archived snapshot tree. |
| `--window-days` |  | `7` |  | Number of day-to-day pairs to walk (default 7). |

### `tools.port_supply_diff`

Diff two Port Supply Lines summary snapshots.

```
python -m tools.port_supply_diff
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `after` |  | — |  | Path to the later summary CSV (e.g. today's export). |
| `before` |  | — |  | Path to the earlier summary CSV (e.g. yesterday's export). |
| `--format` |  | `text` | `text`, `json`, `markdown` | Output format (default: text). |
| `--min-delta` |  | `1.0` |  | Minimum \|deficit_days\| change to count as a 'material' move (default 1.0). Severity-band shifts + watchlist transitions are reported regardless. |

### `tools.port_supply_export`

tools/port_supply_export.py — CLI bulk-export for Port Supply Lines CSVs.

```
python -m tools.port_supply_export
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--container-type` |  | `40FT_DRY` | `40FT_DRY`, `20FT_DRY`, `40FT_HC`, `40FT_REEFER`, `20FT_TANK` | Container-type slice of regional supply state (default: 40FT_DRY). |
| `--json` |  | false |  | Emit a JSON manifest instead of the human-readable text report. Pairs well with --quiet=false for scripting. |
| `--out-dir` |  | `.` |  | Directory to write the CSVs into (default: current dir). |
| `--quiet` |  | false |  | Suppress stdout (useful for cron). Exit code still signals success. |
| `--threshold-days` |  | — |  | Deficit-watchlist firing threshold in days (negative = deficit; default -3.0). |
| `--views` |  | `all` |  | Comma-separated view names to export, or 'all' (default). Valid: {', '.join(ALL_VIEWS)}. |
| `--xlsx` |  | false |  | Also write a single .xlsx workbook (all 5 sheets bundled) alongside the CSV files. Useful for analysts who prefer a single file with sheets they can pivot across. |

### `tools.replay_cli`

Replay historical alerts to a delivery channel without consuming budget.

```
python -m tools.replay_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `<bulk>` |  | — |  | subcommand: bulk |
| `<replay>` |  | — |  | subcommand: replay |

### `tools.schema_docs_cli`

Generate Markdown / JSON documentation of the SQLite schema from a live DB + migration history.

```
python -m tools.schema_docs_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `<history>` |  | — |  | subcommand: history |
| `<json>` |  | — |  | subcommand: json |
| `<markdown>` |  | — |  | subcommand: markdown |

### `tools.snapshot_integrity_cli`

Check port-supply snapshot files for completeness + parse warnings.

```
tools.snapshot_integrity_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--all` |  | false |  | Sweep every snapshot date in the tree. |
| `--container-type` |  | `40FT_DRY` |  | Container type to check (default: 40FT_DRY). |
| `--date` |  | — |  | Check a single ISO date (YYYY-MM-DD). |
| `--format` |  | `text` | `text`, `json` | Output format (default: text). |
| `--root` |  | — |  | Snapshot root override (default: project's cache/). |
| `--since` |  | — |  | With --all, only check dates on-or-after this ISO date. |
| `--verify` |  | false |  | Exit 1 if any snapshot has missing or corrupted files. Designed for CI / cron gating. |

### `tools.styles_audit`

Design-system audit tool.

```
python -m tools.styles_audit
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--out` |  | — |  | CSV output path (default: docs/audit-baseline.csv) |
| `--print` |  | false |  | Also print the top-20 to stdout |
| `--root` |  | — |  | Project root (defaults to parent of tools/) |

### `tools.supply_shock_cli`

Quantify the company-level impact of pre-canned supply-line shocks (Suez closure, Shanghai 50%, Panama drought, etc.).

```
python -m tools.supply_shock_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `--all` |  | false |  | Run every BUILTIN_SCENARIOS in turn. |
| `--container-type` |  | `40FT_DRY` | `40FT_DRY`, `20FT_DRY`, `40FT_HC`, `40FT_REEFER`, `20FT_TANK` | Container-type slice of regional supply state (default: 40FT_DRY). |
| `--format` |  | `text` |  | Output format (default: text). |
| `--out` |  | — |  | Optional file path to write the report to (in addition to stdout). |
| `--scenario` |  | — |  | Run a single named scenario (case-insensitive). Repeat the flag to run a list. |
| `--top-n` |  | `10` |  | Per-scenario, cap on the most-impacted-companies listing (default 10). |

### `tools.test_flakiness_cli`

Track + surface flaky tests from pytest JUnit XML.

```
python -m tools.test_flakiness_cli
```

| Arg | Required | Default | Choices | Help |
| --- | --- | --- | --- | --- |
| `<clear>` |  | — |  | subcommand: clear |
| `<flaky>` |  | — |  | subcommand: flaky |
| `<history>` |  | — |  | subcommand: history |
| `<ingest>` |  | — |  | subcommand: ingest |
| `<run>` |  | — |  | subcommand: run |
| `<slow>` |  | — |  | subcommand: slow |
| `<summary>` |  | — |  | subcommand: summary |
